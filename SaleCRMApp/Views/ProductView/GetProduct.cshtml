@*@{
    ViewData["Title"] = "GetProduct";
}

@using SwadeshiApp.Models
@model List<Product>

<style>
    .card {
        transition: transform 0.2s;
        text-align: center; /* Center-align content */
    }

        .card:hover {
            transform: scale(1.05);
        }

    .card-img-top {
        transition: transform 0.2s;
        
    }

    .card:hover .card-img-top {
        transform: scale(1.2);
    }
</style>
<br></br>
<div class="container-fluid">
    <div class="row">
        @foreach (var product in Model)
        {
            <div class="col-md-4">
                <div class="card mb-4" style="height: 400px; overflow: hidden;">
                    <div class="card-img-top" style="height: 200px; padding-top:20px; overflow: hidden;">
                        <img src="@product.PicturePath" class="img-fluid" alt="@product.Name" style="height: 100%; object-fit: cover;">
                    </div>
                    <div class="card-body">
                        <h3 class="card-title">@product.Name</h3>
                        <p class="card-text"  overflow: hidden;">@product.ShortDescription</p>
                        <h5 class="card-title">@product.UnitPrice</h5>
                        <div class="btn-group">
                            <a href="@Url.Action("AddToCart","Cart",new { @ProductID = product.ProductID})" class="btn btn-primary" style="margin-right:20px;border-radius:10px;">Add to Cart</a>
                            <a href="@Url.Action("Index", "Payment", new { ProductId = product.ProductID, TotalAmount = product.UnitPrice })" class="btn btn-success" style="border-radius:10px;">Buy Now</a>

                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
*@
@{
    ViewData["Title"] = "GetProduct";
}

@using SwadeshiApp.Models
@model List<Product>

<style>

    .card-sl {
        border-radius: 8px;
        box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
        transition: transform 0.3s ease;
        background: linear-gradient(to right, #FFEAC7, #ffffff, #D1FFC7 );
    }

        .card-sl:hover {
            /* box-shadow: 0 8px 16px 0 rgba(0, 0, 0, 0.2);*/
            transform: translateY(-5px) rotateX(5deg) rotateY(5deg);
        }

    .card-image img {
        max-height: 100%;
        max-width: 100%;
        border-radius: 8px 8px 0px 0;
    }

    .card-action {
        position: relative;
        float: right;
        margin-top: -25px;
        margin-right: 20px;
        z-index: 2;
        color: #E26D5C;
        background: #fff;
        border-radius: 100%;
        padding: 15px;
        font-size: 15px;
        box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.2), 0 1px 2px 0 rgba(0, 0, 0, 0.19);
    }

        .card-action:hover {
            color: #fff;
            background: #E26D5C;
            -webkit-animation: pulse 1.5s infinite;
        }

    .card-heading {
        font-size: 18px;
        font-weight: bold;
        background: #fff;
        padding: 10px 15px;
    }

    .card-text {
        padding: 10px 15px;
        background: #fff;
        font-size: 14px;
        color: #636262;
    }

    .card-button {
        display: flex;
        justify-content: center;
        padding: 10px 0;
        width: 100%;
        /*background-color: #1F487E;*/
        color: #fff;
        border-radius: 0 0 8px 8px;
    }

        .card-button:hover {
            text-decoration: none;
           /* background-color: #1D3461;*/
            color: #fff;
        }
    .btn-3d {
        box-shadow: 0px 5px 10px rgba(0, 0, 0, 0.2); /* Add a shadow */
        transition: all 0.3s ease; /* Add transition for smooth effect */
    }

        .btn-3d:hover {
            transform: translateY(-2px); /* Move button up slightly on hover */
            box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.3); /* Increase shadow on hover */
        }
</style>

<br>

@*<div class="container-fluid">
    <div class="row">
        @foreach (var product in Model)
        {
                <div class="col-md-4">
                    <div class="card product-card mb-4" style="height: 400px; overflow: hidden;">
                        <div class="card-img-top" style="height: 200px; padding-top: 20px; overflow: hidden;">
                            <img src="@product.PicturePath" class="img-fluid" alt="@product.Name" style="height: 100%; object-fit: cover;">
                        </div>
                        <div class="card-body">
                            <h3 class="card-title">@product.Name</h3>
                            <p class="card-text" overflow: hidden;">@product.ShortDescription</p>
                            <h5 class="card-title">@product.UnitPrice</h5>
                            <div class="btn-group btn-3d">
                                
                            <a href="@Url.Action("AddToCart","Cart",new { @ProductID = product.ProductID})" class="btn btn-primary" style="margin-right:20px;border-radius:10px;">Add to Cart</a>   
                            <a href="@Url.Action("ViewProduct", "Products", new { ProductId = product.ProductID })" class="btn btn-success" style="border-radius:10px;">View</a>
                            </div>
                        </div>
                    </div>
                </div>
        }
    </div>
</div>*@
<div class="container-fluid" style="margin-top: 25px;">
    <div class="row">
        @foreach (var product in Model)
        {
            <div class="col-md-3">
                <div class="card-sl">
                    <div class="card-image">
                        <img src="@product.PicturePath" class="card-img-top" alt="@product.Name" style="height: 200px; object-fit:contain;margin-top: 25px;">
                    </div>

                    
                    <div class="card-heading">
                        @product.Name
                    </div>
                    <div class="card-text" style="height: 100px; overflow: hidden;">
                        @product.ShortDescription
                    </div>

                    <div class="card-button btn-group">
                        <a href="@Url.Action("AddToCart","Cart",new { @ProductID = product.ProductID})" class="btn btn-primary btn-3d" style="margin-right:20px; margin-left:20px;border-radius:10px;">Add to Cart</a>
                        <a href="@Url.Action("ViewProduct", "Products", new { ProductId = product.ProductID })" class="btn btn-success btn-3d" style="margin-right:20px; border-radius:10px;">View</a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

